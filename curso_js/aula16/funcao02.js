function soma(n1=0, n2=0) { //Pode ser passado um parâmetro "default" atribuindo dentro dos (). Assim caso algum parâmetro não seja passado, ele pega o default e n quebra a função.
    return n1 + n2
}

console.log(soma(3, 5))